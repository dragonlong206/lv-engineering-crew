lark:
  base_id: "xxx"
  table_id: "xxx"
  feature_id_field: "Feature ID"   # tên cột chứa feature ID trong Lark Base

default_branch: main

# Model mặc định nếu không khai báo per-step
model: openai/gpt-4o

# Model theo từng bước — bỏ field để dùng model mặc định
models:
  analysis: openai/gpt-4o
  design: openai/gpt-4o
  bootstrap: openai/gpt-4o-mini
