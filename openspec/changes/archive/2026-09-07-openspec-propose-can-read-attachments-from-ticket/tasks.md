## 1. Config and state schema

- [x] 1.1 Add `attachment_field: z.string().optional()` to `LarkConfigSchema` in `src/types.ts`, with a comment matching the existing field-documentation style, and verify `npx tsc --noEmit` passes
- [x] 1.2 Add `attachments: z.array(z.string()).optional()` to `StateSchema` in `src/types.ts` (repo-relative paths under `docs/changes/<change-id>/attachments/`) and verify `npx tsc --noEmit` passes
- [x] 1.3 Document `lark.attachment_field` in `.lv.yaml` as a commented-out example, following the existing style of `ui_design_field` and the other optional `lark.*` fields

## 2. Lark ticket extraction

- [x] 2.1 Add an `extractAttachments(value): { fileToken: string; name: string }[]` helper in `src/tools/lark.ts` that reads a Lark attachment-field's array value and, per entry, pulls `file_token` and `name` — skipping any entry missing `file_token` — and verify with a quick `npx tsx` script exercising a sample payload shape
- [x] 2.2 Add `attachments: { fileToken: string; name: string }[]` to the `LarkTicket` interface in `src/tools/lark.ts`
- [x] 2.3 Extend `fetchTicket()` to accept the configured `attachmentField` name (mirroring `uiDesignField`), read `fields[attachmentField]` when the name is given, extract it via `extractAttachments()`, and set `attachments` (empty array when the field name isn't provided or resolves to nothing) — no download in this function
- [x] 2.4 Update `fetchTicket()`'s call site(s) in `src/cli/start.ts` (and the `larkTicketTool` Mastra tool definition, if it also needs the new parameter) to pass `config.lark.attachment_field`, and verify `npx tsc --noEmit` passes

## 3. Download attachments in `lv start`

- [x] 3.1 Add `downloadTicketAttachments(attachments, destDir, token): Promise<{ downloaded: string[]; failed: { name: string; error: string }[] }>` in `src/tools/lark.ts` — for each attachment, `GET https://open.larksuite.com/open-apis/drive/v1/medias/:file_token/download` with `Authorization: Bearer <token>`, writing the response body to `destDir`; sanitize each `name` (strip path separators/unsafe characters) and de-duplicate collisions within the same call (`file.png`, `file-2.png`, ...) before writing
- [x] 3.2 Make per-file failures independent: catch each download's error individually, collect it into `failed` rather than throwing, so one bad `file_token` doesn't abort the rest of the batch
- [x] 3.3 Verify `downloadTicketAttachments()` directly against a small local fixture (e.g. a stubbed fetch or a real test attachment if a scratch Lark ticket is available) — confirm files land under the given `destDir`, name collisions are de-duplicated, and a simulated failure is reported in `failed` without throwing — verified via a scratch `npx tsx` script stubbing `global.fetch`: writes both files, dedupes `screenshot.png`/`screenshot-2.png`, reports the failing entry in `failed` without throwing, and no-ops (no directory created) on empty input

## 4. Wire into `lv start` and persist to state.yaml

- [x] 4.1 In `startFromTicket()` (`src/cli/start.ts`), after `fetchTicket()` returns, call `downloadTicketAttachments()` when `ticket.attachments.length > 0`, targeting `path.join(getChangesDir(repoRoot, ticketId), 'attachments')` (create the directory as needed), using the existing `larkToken`
- [x] 4.2 Print a warning per entry in `downloadTicketAttachments()`'s `failed` list (naming the file), matching the non-fatal posture of `sync_status`/`sync_feature_id` — this must not stop `lv start` from continuing
- [x] 4.3 Set `state.attachments` from the successfully downloaded files' repo-relative paths (e.g. `docs/changes/<ticketId>/attachments/<name>`) only when non-empty (omit the key otherwise), alongside the existing `ui_design` handling
- [x] 4.4 Verify locally via `StateSchema.parse()` + `yaml.dump()` (matching `writeState()`'s own path) that: a ticket with downloaded attachments produces a `state.yaml` with the `attachments` key listing their paths; a ticket with no attachments (or `attachment_field` unset) produces a `state.yaml` with no `attachments` key at all — same verification approach `ui-design-capture` used, since a live `lv start` run has real side effects (branch/commit/Lark write-back) on a real ticket

## 5. Patch `/opsx:propose` to read downloaded attachments, via `lv init`

- [x] 5.1 Add `PROPOSE_ATTACHMENT_LINE` to `src/prompts.ts`: instructs the workflow that, specifically when creating the `proposal` artifact (not the other artifact types), it should read each file listed in `state.yaml`'s `attachments` directly (already a local repo path — no fetch needed) and reflect what it observes in `proposal.md`'s "What Changes"/"Impact" sections, falling back to citing the file's name/path when its format can't be read directly (e.g. a video)
- [x] 5.2 Add `addProposeAttachmentInstruction()` to `src/cli/init.ts`, patching `PROPOSE_WORKFLOW_FILES` at the same anchor `addProposeStateAutoload()`/`addProposeUiDesignInstruction()` use (step 1's "ask the user" line), idempotent like the other propose patches; call it from `runInit()` alongside the existing propose patches
- [x] 5.3 Verify the patch logic against scratch copies of the real `.claude/commands/opsx/propose.md`/`.claude/skills/openspec-propose/SKILL.md`/`.agents/skills/openspec-propose/SKILL.md`: run 1 inserts the new line into all three (anchor found, correct text), run 2 detects "already patched" (idempotent) in all three — verified by temporarily exporting `addProposeAttachmentInstruction()`, running it against scratch-directory copies of the three real files, then reverting the export
- [x] 5.4 Apply the patch for real to this repo's own three propose workflow files (safe: pure text insertion, no install/network/git side effects) — done directly rather than via a full `lv init` re-run, since that would also re-trigger the OpenSpec CLI install/config flow. Applied to `.claude/commands/opsx/propose.md`, `.claude/skills/openspec-propose/SKILL.md`, and `.agents/skills/openspec-propose/SKILL.md`, immediately after the existing `ui_design` line
- [x] 5.5 Verify end-to-end: run `/opsx:propose` on a branch whose `state.yaml` has real downloaded attachments (at least one readable image/PDF, and — if feasible — one unreadable format like a video) and confirm the generated `proposal.md` reflects actual observed content for the readable file(s) and cites name/path for the unreadable one, rather than fabricating either — verified against real ticket `recvupmz8I8Fhe` (user-provided, "fake attachment file"): `fetchTicket()` with `attachmentField: "Attachment"` extracted `{ fileToken: "YU1SbsB6UoXfEax5FoklmXhGgoe", name: "SKILL.md" }`, and `downloadTicketAttachments()` downloaded it via the real Drive media-download endpoint with byte-for-byte correct content (7965 bytes, readable markdown), confirming the `url` shape Lark actually returns matches the endpoint this code constructs from `file_token`. Ran directly against the live ticket rather than through a full `lv start` (would have swept this session's uncommitted implementation changes into an unrelated ticket branch's commit/push and written back to a real ticket's status field) — the patched propose-workflow instruction itself was already verified separately in 5.3/5.4; no unreadable-format (video) attachment was available on this ticket to exercise that fallback

## 6. Documentation

- [x] 6.1 Run `lv bootstrap lv-start` (or otherwise refresh `docs/features/lv-start/{overview.md,design.md}`) to reflect the new attachment capture behavior, then review and commit manually per this repo's convention — ran `npm run dev -- bootstrap lv-start`; refreshed docs correctly reflect `attachment_field`, `attachments`, `downloadTicketAttachments()`, and the new `fetchTicket()` signature. Files written, not committed (per this repo's convention) — review and commit manually
