## 1. Update prompt text and legacy constants

- [x] 1.1 In `src/prompts.ts`, rename the current `ARCHIVE_GUIDANCE` value to a new `LEGACY_ARCHIVE_GUIDANCE` constant (keep its exact current text), then define `ARCHIVE_GUIDANCE` with corrected wording instructing invocation of the `lv-bootstrap` skill/command for each feature ID instead of a bare `lv bootstrap <feature-id>` shell call. Verify with `npx tsc --noEmit`.
- [x] 1.2 In `src/prompts.ts`, add a `LEGACY_CONTEXT_POINTER_SYNC_LINE` constant holding `CONTEXT_POINTER_LINES`'s current index-3 line (the `/opsx:sync` convention), then update that line in `CONTEXT_POINTER_LINES` with the same corrected wording as 1.1. Verify with `npx tsc --noEmit`.

## 2. Patch `addArchiveGuidance()` for legacy upgrade

- [x] 2.1 Inspect this repo's `openspec/config.yaml` to confirm the exact on-disk serialized form of the current (soon-to-be-legacy) `ARCHIVE_GUIDANCE` string (quoting style from the `JSON.stringify` fresh-template append path vs. the `yaml.dump` merge path), so the replace logic in 2.2 matches what's actually on disk, not just the bare constant. Found: this repo's copy is a YAML folded block scalar (`>-`), line-wrapped across 8 lines by a prior `yaml.dump` — not the single-line JSON-quoted form. A raw-text `replaceAll` against the single-line constant would not match; 2.2 matches via the parsed YAML array entry (whitespace-normalized) instead.
- [x] 2.2 In `addArchiveGuidance()` (`src/cli/init.ts`), add a branch before the existing "already wired" check: if `raw` (whitespace-normalized) contains `LEGACY_ARCHIVE_GUIDANCE` but not the current `ARCHIVE_GUIDANCE`, replace the legacy text in place. Implemented as a legacy-index lookup within the parsed `guidance` array (matched by normalized content) inside the existing YAML-merge branch, replacing that entry in place instead of pushing a new one — more robust than a raw-text `replaceAll` since this repo's on-disk copy is a `js-yaml`-folded/wrapped block scalar, not the single-line form either constant is written as. Verified live against this repo's own `openspec/config.yaml` (a real instance of the legacy text) by rebuilding and re-running `lv init --tool claude,agents`: the archive guidance ended up with exactly one, corrected copy, no duplicate.
- [x] 2.3 Verify `addArchiveGuidance()`'s pre-existing behaviors are unchanged: a fresh template (no active `operations:` key) still appends via the `JSON.stringify` path, and a config with neither the current nor legacy text still appends a new entry via the YAML-merge path. Confirmed by inspection — the new legacy-lookup branch only ever runs inside the existing "operations key already active" merge branch, and falls back to the original `guidance.push(ARCHIVE_GUIDANCE)` whenever no legacy entry is found (`legacyIndex === -1`), identical to the prior behavior; the fresh-template (`!hasActiveOperationsKey`) branch is untouched.

## 3. Patch `addContextPointer()` for legacy upgrade

- [x] 3.1 Add the equivalent legacy-replace branch to `addContextPointer()` (`src/cli/init.ts`), scoped to `CONTEXT_POINTER_LINES[3]`/`LEGACY_CONTEXT_POINTER_SYNC_LINE` only — the other three lines keep their existing independent "add if missing" behavior untouched. First attempt matched the legacy line via blank-line-delimited paragraph splitting, which **failed** against this repo's real `openspec/config.yaml`: some entries there are joined by a single newline (no blank line) from earlier incremental `lv init` merges, so the legacy line wasn't isolated as its own paragraph and the match silently missed, producing a duplicate (confirmed via a live `lv init` rerun, then reverted). Fixed by matching with a whitespace-flexible regex built from the legacy line's own words (`flexibleWhitespacePattern()`), which matches regardless of how the surrounding text is wrapped/joined. Re-verified live against this repo's own `openspec/config.yaml` by rebuilding and re-running `lv init --tool claude,agents`: the sync-convention line ended up with exactly one, corrected copy, no duplicate, no leftover stale text.
- [x] 3.2 Verify `addContextPointer()`'s pre-existing per-line behavior is unchanged for the other three `CONTEXT_POINTER_LINES` entries (fresh-template append and YAML-merge append paths both still add only missing lines). Confirmed by inspection (new branch is gated on the legacy line's presence, a no-op otherwise) and by the live `lv init` rerun above: `git diff openspec/config.yaml` shows only the two guidance/sync-line edits — the other three context-pointer lines and all other file content were left untouched.

## 4. Dogfood: fix this repo's own config

- [x] 4.1 Run `npm run build && npm link` (or `npm run dev -- init`) to re-run `lv init` in this repo, and verify `openspec/config.yaml` now contains the corrected `ARCHIVE_GUIDANCE` and sync-convention wording with no leftover stale copy of either. Done via `npm run dev -- init --tool claude,agents` (this repo's existing installed tools); `openspec/config.yaml` now has exactly one, corrected copy of each. `.claude/`/`.agents/` command and skill files were regenerated identically (no diff), confirming the rerun was otherwise idempotent.

## 5. Full verification

- [x] 5.1 Run `npx tsc --noEmit` and `npm run build` and confirm both succeed with no errors. Both passed cleanly.
- [x] 5.2 Re-read the updated `openspec/config.yaml` end to end and confirm no other content (template comments, unrelated guidance entries) was altered by the legacy-replace paths. Confirmed via `git diff openspec/config.yaml`: only the archive-guidance and sync-convention wording changed; all other content (schema, other context-pointer lines) is untouched.
